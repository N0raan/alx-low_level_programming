#include <stdio.h>

/**
 * main - print its name
 * @argc : arguments counter
 * @argv : arguments values
 * Return : Always 0
 */

int main(int argc, char *argv[])
{
printf("%s \n", argv[argc-1]);
return(0);
}
