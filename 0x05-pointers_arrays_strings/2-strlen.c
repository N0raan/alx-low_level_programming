#include <stdio.h>
#include "main.h"

/**
* _strlen - return string length
* @s :pointer string
* Return: string length
*/

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
int sum;
sum = sum + i;
}
return (sum);
}
